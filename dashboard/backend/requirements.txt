# Dashboard backend deps. Reuses the repo's existing onnxruntime-gpu / pyzed /
# opencv / pymavlink / tensorrt install — do NOT reinstall those here.
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=11.0
pydantic>=2.0
psutil>=5.9
# Optional, Jetson only, for GPU load + component temperatures:
# jetson-stats>=4.2
